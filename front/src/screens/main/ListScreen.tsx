import React from 'react';
import { StyleSheet, View } from 'react-native';

interface ListScreenProps {}

function ListScreen({}: ListScreenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default ListScreen;
