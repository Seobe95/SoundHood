import React from 'react';
import { StyleSheet, View } from 'react-native';

interface PostScreenProps {}

function PostScreen({}: PostScreenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default PostScreen;
