import React from 'react';
import { StyleSheet, View } from 'react-native';

interface EditScreenProps {}

function EditScreen({}: EditScreenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default EditScreen;
