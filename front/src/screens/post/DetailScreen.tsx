import React from 'react';
import { StyleSheet, View } from 'react-native';

interface DetailScreenProps {}

function DetailScreen({}: DetailScreenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default DetailScreen;
